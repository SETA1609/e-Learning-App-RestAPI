spring:
  datasource:
    username: myuser
    password: secret
    url: //postgresql://localhost:5432/mydatabase
  sql:
    init:
      mode: always
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
server:
  port: 8081
